use iced::widget::canvas::Cache;

// functions to:
// - calculate vector length/ magnetic strength
// to be presented along the charts

struct CurrentValue2DChart {
    cache: Cache,
}

// TODO later
struct Direction3DChart {
    cache: Cache,
}